package global.common.ui.graphing.bar_chart

import androidx.compose.ui.graphics.Color

data class BarScrollButtonStyle(
    val color: Color = Color.Black,
    val bgColor: Color = Color.White.copy(alpha = .5f),
)